#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/* main - entry point */
/* @numbers: digit numbers base 10 */
/* Return: Return 0 value after execution */

int main(void)
{
	int numbers;

	for (numbers = 0; numbers < 10; numbers++)
	printf("%d", numbers);
	putchar('\n');

	return (0);
}
