#include "main.h"
/**
 * _abs - computes absolute value of an integer
 *@wale: the parameter to be examined
 * Return: return _abs
 */
int _abs(int wale)
{
	return (wale * ((wale > 0) - (wale < 0)));
}
