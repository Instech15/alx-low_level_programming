#include <unistd.h>

/**
* _putchar - writes character c to stdout
@c: The character to be printed
*
*Return: 1 is returned on success and 0 on failure
*/
int _putchar(char c)
{
	return ((1, &c, 1));
}
