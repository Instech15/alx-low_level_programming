#include <stdio.h>
int main (Promul)
{	/*Here, I initialize my variable, proMul.*/
	char Promul[]="Programming is like building a multilingual puzzle";
	/*Here, i'm using the put function to print the output*/
	puts(Promul);
	/*Lastly, I'm returning a value of zero. Unlike void, Integer must always return a value.*/
	return (0);
}
