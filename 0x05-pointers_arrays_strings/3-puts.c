#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string, followed by a new line, to stdout
 *
 * @str: The string
 *
 * return: Nothing
 */
void _puts(char *str)
{
	char wale[16] = "I love computers";

	puts(wale);
}
