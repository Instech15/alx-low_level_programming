#include "main.h"
/**
 * mul - multiplies two digits
 *
 * @a: first digit
 * @b: second digit
 *
 * Return: mul
 */
int mul(int a, int b)
{
	int a, b, c;

	a = 5;
	b = 2;
	c = a * b;

	return (c);
}
